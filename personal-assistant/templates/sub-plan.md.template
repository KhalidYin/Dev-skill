---
phase_index: 0
status: planning
created: YYYY-MM-DD
updated: YYYY-MM-DD
priority: 1
estimated_rounds: 4-8
depends_on: []
tags: []
syncs_to:
  - PROJECT_SPEC.md
  - PROJECT_GUIDE.md
  - TEST_GUIDE.md
---

# [功能/修复/模块名称]

> Lifecycle rule: this file's directory and `status` must match.
> `plans/backlog/` = `planning`; `plans/ongoing/` = `in-progress`; `plans/complete/` = `done`; `plans/deferred/` = `deferred`.
> Use `tags` for keywords such as auth/api/security; do not create keyword directories.

## 目标

[一句话：要构建或修复什么，为什么]

## 背景

- 当前状态：[现在怎么做 / 没有做什么]
- 约束：[技术约束、时间约束、依赖约束]
- 方案来源：[正式头脑风暴 / 轻量讨论 / Review / 已有明确需求]
- 头脑风暴记录：[如适用，引用 Storm-R 的结论摘要；否则写“无”]

## 涉及范围

- **包含**：[明确要做的事]
- **不包含**：[明确不做的事]

## 主文档影响

完成后需要更新：

- `PROJECT_SPEC.md`：[新增/修改的具体章节]
- `PROJECT_GUIDE.md`：[新增/修改的具体章节]
- `TEST_GUIDE.md`：[新增/修改的具体章节]
- `CODE_STYLE.md`：[如果有新约定，否则删除此行]

`syncs_to` 和本节必须一致：frontmatter 列出的每个文档都要在这里写明目标章节；这里列出的每个文档也必须出现在 `syncs_to`。

---

## 设计基线与偏差清单（UI 子计划必填；非 UI 删除）

- **设计基线**：[设计稿/截图/现有页面/用户确认的文字需求]
- **版本或日期**：[URL、文件路径、版本号或确认日期]
- **视觉结构**：[模块顺序、首屏默认内容、共享 KPI、卡片/图表/表格布局]
- **窄屏原则**：[重排、折叠、横向滚动或隐藏规则]

| 偏差 ID | 基线 ID | 原设计 | 调整方案 | 调整原因 | 用户确认 |
|---------|---------|--------|----------|----------|----------|
| D-01 | UI-03 | [原设计行为] | [拟调整行为] | [证据或约束] | pending / approved YYYY-MM-DD / rejected |

> `pending` 偏差不能进入实现。没有设计稿时，明确写出替代基线，不得虚构设计依据。

## 页面/组件/状态/交互矩阵（UI 子计划必填；非 UI 删除）

| UI ID | 页面/区域 | 用户可见内容或操作 | 数据来源 / 证据 | 默认状态 | 交互与结果 | 状态覆盖 | 验收断言 | 偏差 |
|-------|-----------|--------------------|-----------------|----------|------------|----------|----------|------|
| UI-01 | [页面/区域] | [看到什么/能做什么] | `payload.path` / 静态配置 / 无 | [首屏表现] | [点击/选择/筛选后的结果及 URL 行为] | 默认/加载/空/错误/部分/窄屏 | [可自动化验证的行为] | 不允许 / D-01 |

规则：
- 每个数字、图表和分组必须声明数据来源；无证据时写明占位、隐藏、禁用或错误提示。
- 禁止计划外推导、补值或合并数据。
- 六类状态必须分别写出具体 UI 表现；不适用时写 `N/A` 及理由。

## 视觉与行为验收清单（UI 子计划必填；非 UI 删除）

- [ ] `[UI-XX]` 首屏模块顺序、默认内容和信息层级与基线一致
- [ ] `[UI-XX]` 核心点击、选择、筛选、分组或导航产生合同指定结果
- [ ] `[UI-XX]` URL 可恢复需要持久化的筛选和视图状态
- [ ] `[UI-XX]` 默认、加载、空数据、错误、部分数据和窄屏状态均已核验
- [ ] 所有设计偏差均已记录且为 `approved`
- [ ] 行为测试覆盖核心操作结果，不只检查标题或静态文本存在

> 每个 UI Phase 的完成标准必须引用其负责的 `UI ID` 和本清单条目。Phase Gate 同时核对设计基线、计划、实现和测试。

---

## Phase 总览

| Phase | 目标 | 预估轮次 | 依赖 | 状态 |
|-------|------|----------|------|------|
| P1 | [一句话目标] | RXXX-RXXX | - | pending |
| P2 | [一句话目标] | RXXX-RXXX | P1 | pending |

---

## P1: [Phase 名称]

### 输入条件

- [开始本 Phase 前必须为真的条件]

### 产出

- [具体的、可验证的交付物]

### 完成标准

- [ ] [可客观验证的标准]
- [ ] [可客观验证的标准]
- [ ] [可客观验证的标准]
- [ ] [UI Phase：引用负责的 UI ID 和视觉/行为验收项；非 UI 删除]

### 边界（本 Phase 明确不做）

- [防止 AI 在执行中越界的排除项]
- [防止 AI 在执行中越界的排除项]

### 涉及文件

| 文件 | 操作 | 预计行数 |
|------|------|----------|
| `path/to/file.py` | 新建 | ~N |
| `path/to/other.py` | 修改 | +N |

### 关键决策

- [本 Phase 的技术选择]：[选项 A / 选项 B]，选择 [X]，理由：[具体理由]
- 如果本 Phase 没有决策点，写“无”

---

## P2: [Phase 名称]

### 输入条件

- [开始本 Phase 前必须为真的条件]

### 产出

- [具体的、可验证的交付物]

### 完成标准

- [ ] [可客观验证的标准]
- [ ] [可客观验证的标准]
- [ ] [可客观验证的标准]
- [ ] [UI Phase：引用负责的 UI ID 和视觉/行为验收项；非 UI 删除]

### 边界（本 Phase 明确不做）

- [防止 AI 在执行中越界的排除项]
- [防止 AI 在执行中越界的排除项]

### 涉及文件

| 文件 | 操作 | 预计行数 |
|------|------|----------|
| `path/to/file.py` | 修改 | +N |

### 关键决策

- 无

---

## 执行中发现

> 执行本子计划过程中暴露的问题。每个 Phase Gate 时审查并分类。

| ID | 描述 | 发现于 | 类型 | 处理 |
|----|------|--------|------|------|
| D1 | [问题描述] | P2 | 阻断/增强/延后 | -> [处理方式] |

## 关键决策记录

| 日期 | 决策 | 选项 | 选择 | 理由 |
|------|------|------|------|------|
| YYYY-MM-DD | [具体决策] | [A / B / C] | [X] | [为什么] |

## 同步记录

| 日期 | 已同步到 | 说明 |
|------|----------|------|
| YYYY-MM-DD | PROJECT_SPEC.md, PROJECT_GUIDE.md | [同步章节和摘要] |
